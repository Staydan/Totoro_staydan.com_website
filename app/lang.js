// var config = require('./config');
// var path = require('path');
// var fs = require('fs');

var lang = {
    getLangsContent: function () {
        // var langFilePath = path.join(__dirname + '/i18n/' + config.default_lang + '.json');
        // console.log(langFilePath);
        // var languageContent = fs.readFileSync(langFilePath);
        // return languageContent;
    }
}
module.exports = lang;
